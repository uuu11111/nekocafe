# Trivy 漏洞豁免清单（每条须写明：CVE、理由、复查到期日）。
# 仅用于「经评估在本服务运行期不可达 / 暂无可行即时修复」的传递依赖漏洞；绝不整体降级阈值。
# 复查到期日统一：2026-09-30（随下一次依赖升级迭代重新评估）。

# ── @opentelemetry/exporter-prometheus（CVE-2026-44902，恶意 HTTP 致进程崩溃）──
# sdk-node 的传递依赖；member 用 prom-client 暴露 /metrics、仅用 OTLP/HTTP 上报链路，
# 从未实例化 OTel 的 Prometheus 导出器 → 漏洞代码路径不可达。修复需 otel 实验包跳级（0.57→0.217）。
CVE-2026-44902

# ── 构建期工具类传递依赖（member 为无状态 Express API，运行期不调用以下能力）──
# cross-spawn 正则 DoS：member 运行期不 spawn 任何子进程 → 不可达。
CVE-2024-21538
# glob 经恶意文件名命令注入：member 运行期不对不可信输入做文件 glob → 不可达。
CVE-2025-64756
# minimatch 经构造 glob 模式 DoS（3 条）：member 运行期不以不可信输入跑 minimatch → 不可达。
CVE-2026-26996
CVE-2026-27903
CVE-2026-27904
# node-tar 路径穿越 / 任意文件写（6 条）：member 运行期不解压任何 tar 归档 → 不可达。
CVE-2026-23745
CVE-2026-23950
CVE-2026-24842
CVE-2026-26960
CVE-2026-29786
CVE-2026-31802
